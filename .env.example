# Copy to .env.local before running the frontend.

# Base URL of the Spring Boot backend (no trailing slash). Falls back to
# http://localhost:8080 in lib/config.ts if unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
